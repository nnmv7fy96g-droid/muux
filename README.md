# Movie Updates Unified — Schedule Site

Auto-generated static site. `index.html` is a copy of `cinema_schedule.html`
produced by the cine_scraper project (private repo, not this one).

This repo is deployed via GitHub Pages at schedule.muux.com.
Updated daily by `daily_scrape.sh` in the main project.
